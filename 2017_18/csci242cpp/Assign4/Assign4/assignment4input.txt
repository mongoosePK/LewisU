push 1
push 2
push 3
pop
push 4
push 5
push 6
pop
push 7
push 8

//std::queue<int>* p1;
	//std::queue<int>* p2;
	//(q1.empty() ? p1 = &q2 : p1 = &q1); // make p1 point to populated container
	//(q1.empty() ? p2 = &q1 : p2 = &q2); // make p2 point to empty container

	//for (size_t i = 0; i < p1->size(); i++) // move all elements to empty q 
	//{
	//	p2->push(p1->front());
	//	p1->pop();
	//}
	//p1->pop();